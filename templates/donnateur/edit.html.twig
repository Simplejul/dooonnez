{% extends 'base.html.twig' %}

{% block title %}Edit Donnateur{% endblock %}

{% block body %}
    <h1>Edit Donnateur</h1>

    {{ include('donnateur/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('donnateur_index') }}">back to list</a>

    {{ include('donnateur/_delete_form.html.twig') }}
{% endblock %}
